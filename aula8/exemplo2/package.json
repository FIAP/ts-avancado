{
  "name": "aula8-exemplo2",
  "version": "1.0.0",
  "description": "Aula 8 — LangChain + Ollama (LLM local) + MongoDB (contexto e histórico)",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "tsx src/app.ts",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down"
  },
  "license": "ISC",
  "dependencies": {
    "@langchain/core": "^1.1.46",
    "@langchain/ollama": "^1.2.7",
    "mongodb": "^6.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0"
  }
}
